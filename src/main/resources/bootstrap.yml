spring:
  cloud:
    consul:
      config:
        enabled: true
        prefix: config
        default-context: TestConsulApplication
        profile-separator: '::'
        watch:
          enabled: true
      discovery:
        service-name: TestConsulApplication
      host: 192.168.99.100


